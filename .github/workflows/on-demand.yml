name: VM
on:
  pull_request:
    types: [opened, reopened, synchronize, labeled]

defaults:
  run:
    working-directory: packages/vm

jobs:
  # test-vm-state:
  #   # if: contains(github.event.pull_request.labels.*.name, 'pr: test all hardforks')
  #   runs-on: ubuntu-latest
  #   strategy:
  #     matrix:
  #       fork: [
  #         'Berlin',
  #         'Istanbul',
  #       ]
  #     fail-fast: false
  #   steps:
  #     - uses: actions/setup-node@v1
  #       with:
  #         node-version: 12.x

  #     - run: echo $'{{ matrix.fork }}'

  #     - run: ${{ toJson(github) }}

  test-vm-state-2:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        fork: [
          'Berlin',
          'Istanbul',
        ]
      fail-fast: false
    steps:
      - uses: actions/setup-node@v1
        with:
          node-version: 12.x

      - run: ${{ github.event.action }}
        continue-on-error: true

      - run: ${{ toJson(github) }}
        continue-on-error: true

      - run: ${{ toJson(github.event.pull_request.labels.*.name) }}
        continue-on-error: true
      
      # - run: ${{ contains(github.event.pull_request.labels.*.name, "pr:\u0020test all hardforks") }}
      #   continue-on-error: true
      
      - run: ${{ contains(github.event.pull_request.labels.*.name, "pr&#58;&#x20;test all hardforks") }}
        continue-on-error: true
      
      - run: ${{ matrix.fork }}
        continue-on-error: true

      # Escaping : in yaml is :: (smh)
      - run: echo "pr:\ asd"

